main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ Vault, EssentialsX, AspeKt ]
libraries: ${libraries}
commands:
  market:
  ah:
  auctionhouse:
  aauc:
  amarketreload:
  amarket: